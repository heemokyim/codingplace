1. pip install pyinstaller

2. pyinstaller --onefile frontend.py
if Windows or Mac, add --windowed

3. where is created file?
==> it'll print out path when done

4. How to add existing database?
put the rightful *.db in same directory
