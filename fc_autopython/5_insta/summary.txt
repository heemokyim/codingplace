어떤 웹사이트는 4_crawling처럼 태그만으론 조작이 불가능한 경우 있음
대표적으로 인스타그램

이럴 땐 사람이 하는 방식을 최대한 모방해서 자동화

--------------------------------------------------------

chromedriver는 복사해서 쓰면 권한문제뜸
==> 폴더 안에 일일이 압축 풀어서 사용할 것

--------------------------------------------------------

** input tag의 class name 빨리 찾는 법
1. 검사
2. 컨트롤 쉬프트 c
3. 검색창에다 입력하면 오른쪽에 태그가 채워짐
4. 그놈의 class 이름

--------------------------------------------------------

implicitly wait를 쓰면 최대 threshold를 지정해서 쓸 수 있음
time.wait(2)면 뭐가 문젤까?
2초가 지난 후에도 페이지가 안뜨면 프로그램 오류

--------------------------------------------------------

1. time.wait 말고 implicitly wait

2. 각 Action마다 코드 모듈화

--------------------------------------------------------

만약 다른 웹페이지를 임베드시킨 경우 = iframe
이 땐 드라이버가 iframe으로 선언된 페이지를 못 읽음
==> switch_to_frame사용

--------------------------------------------------------

Not clickable error?
예를 들어 셀레니움한테 다음 페이지로 넘어가라고 하고 싶음
근데 화면에 다음 페이지 버튼이 안보이면 나는 에러
